{
  "development": {
    "username": "root",
    "password": "mysql",
    "database": "crudtest_db",
    "host": "localhost",
    "dialect": "mysql",
    "pool": {
      "max": 10,
      "idle": 10000
    },
    "databaseurl": "mysql://localhost:3306/crudtest_db",
    "synchronize": true,
    "logging": true,
    "operatorsAliases": {
      "$and": "Op.and",
      "$or": "Op.or",
      "$eq": "Op.eq",
      "$gt": "Op.gt",
      "$lt": "Op.lt",
      "$lte": "Op.lte",
      "$like": "Op.like"
    }
  },
  "test": {
    "username": "root",
    "password": "root",
    "database": "testdb",
    "host": "localhost",
    "dialect": "postgres",
    "entities": ["name", "price", "categotry"],
    "synchronize": true,
    "logging": false,
    "operatorsAliases": {
      "$and": "Op.and",
      "$or": "Op.or",
      "$eq": "Op.eq",
      "$gt": "Op.gt",
      "$lt": "Op.lt",
      "$lte": "Op.lte",
      "$like": "Op.like"
    }
  },
  "production": {
    "username": "root",
    "password": "mysql",
    "database": "crudtest_db",
    "host": "localhost",
    "dialect": "mysql",
    "databaseurl": "mysql://localhost:3306/crudtest_db",
    "synchronize": true,
    "logging": false,
    "operatorsAliases": {
      "$and": "Op.and",
      "$or": "Op.or",
      "$eq": "Op.eq",
      "$gt": "Op.gt",
      "$lt": "Op.lt",
      "$lte": "Op.lte",
      "$like": "Op.like"
    }
  }
}
